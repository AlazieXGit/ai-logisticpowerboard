# LoadBoard AI Development Environment
# Use with direnv: https://direnv.net/

if ! has nix_direnv_version || ! nix_direnv_version 2.3.0; then
  source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/2.3.0/direnvrc" "sha256-Dmd+j63L84wuzgyjITIfSxSD57Tx7v51DMxVZOsiUD8="
fi

use flake

# Load additional environment variables if needed
export NODE_ENV=development
export VITE_PORT=8080
export PREVIEW_PORT=8000
export CAPRA_PORT=9000

echo "✅ Development environment loaded"
echo "📝 Run 'supervisorctl status' to check service status"
